<!DOCTYPE html>
<html>
<head>
  <title>ImgurClone</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="flash-notice"><%= flash.notice %></div>

  <nav class="top-bar">

    <ul class="top-left">
      <li><%= link_to "imgurClone", root_url %></li>
      <li><%= link_to "Upload an Image", new_image_url %></li>
    </ul>
    <ul class="top-right">
      <% if current_user %>
        <li><%= button_to "Log out", session_url, method: :delete if current_user %></li>
        <li><%= link_to current_user.email, user_url(current_user) if current_user %></li>
      <% else %>
        <li><%= link_to "Log In", new_session_url unless current_user %></li>
        <li><%= link_to "Sign Up", new_user_url unless current_user %></li>
      <% end %>
    </ul>
  </nav>

<div class="content">
  <%= yield %>
</div>

</body>
</html>
