<%= render "comments/comment_body", image_id: image_id, comment: comment %>
<ul>
  <% comments_by_parent_id[comment.id].each do |comment| %>
    <li>
      <%= render "comment", image_id: image_id, comment: comment, comments_by_parent_id: comments_by_parent_id %>
    </li>
  <% end %>
</ul>