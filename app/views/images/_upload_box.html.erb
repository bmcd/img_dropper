<%= form_for image, url: { action: "create" }, html: { id: 'new-image' } do |f| %>
  Upload an image:<br>
  <%= f.file_field :image %><br>
  ...or paste a URL:<br>
  <%= f.text_field :image_url %><br>

  <%= f.submit "Upload" %>
<% end %>